const tech = 'Node.js'

const array = ['Node.Js', 'React', 'TypeScript']

const includesString = tech.includes('Node.') // Case Sensitive

const includesArray = array.includes('React') // Procurando pelo valor exato no array. 

// Startswith e Endswith

const startsWith = tech.startsWith('Node')

const endsWith = tech.endsWith('ode.js')

console.log(startsWith) 
console.log(endsWith) 
